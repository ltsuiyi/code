<?php
// 这是系统自动生成的公共文件
function implodeArr($str){
    if (empty($str)) return '';
    return implode(',',$str);
}

function explodeArr($str){

    if (empty($str)) return '';
    return explode(',',$str);
}